<section id="categories">
    <section id="category-card">
        <h2>Categories</h2>
        {{#each categories as |category|}}
        {{> category-details}}
        {{/each}}
    </section>
</section>